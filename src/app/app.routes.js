(function () {
  'use strict';
  angular.module('MainApp').run(mainAppRun);

  mainAppRun.$inject = ['$rootScope', '$injector', '$state', 'PolyglotService'];
  function mainAppRun($rootScope, $injector, $state, PolyglotService) {

  }
}) ();